{
  "name": "cryptovision-pro",
  "version": "1.0.0",
  "private": true,
  "description": "Next-Generation Cryptocurrency Intelligence & Market Analytics Platform",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "npm run build:api && npm run build:web",
    "build:shared": "npm run build --workspace=@cryptovision/shared-types",
    "build:api": "npm run build:shared && npm run prisma:generate --workspace=@cryptovision/api && npm run build --workspace=@cryptovision/api",
    "build:web": "npm run build:shared && npm run build --workspace=@cryptovision/web",
    "start:api": "node apps/api/dist/main.js",
    "start:web": "npm run preview --workspace=@cryptovision/web -- --host 0.0.0.0",
    "start": "npm run start:api",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "docker:up": "docker-compose -f infra/docker/docker-compose.yml up -d",
    "docker:down": "docker-compose -f infra/docker/docker-compose.yml down",
    "prisma:generate": "npm run --prefix apps/api prisma:generate",
    "prisma:migrate": "npm run --prefix apps/api prisma:migrate"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "prettier": "^3.2.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
